# 18_synthetic_did.R — esqueleto (primeira entrega 2026-09-03).
# Contrato de interface definido; corpo ativado por fase (ver _targets.R).

run_synthetic_did <- function(panel, cfg) stop('Fase 2: synthdid — unidade tratada única (Trindade)')
