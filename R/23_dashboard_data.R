# 23_dashboard_data.R — esqueleto (primeira entrega 2026-09-03).
# Contrato de interface definido; corpo ativado por fase (ver _targets.R).

build_dashboard_data <- function(panel, registry) stop('Fase 2: agregados p/ Shiny (app/app.R)')
