# 15_event_study.R — esqueleto (primeira entrega 2026-09-03).
# Contrato de interface definido; corpo ativado por fase (ver _targets.R).

run_event_study <- function(panel, cfg) stop('Fase 2: fixest::feols com sunab/iplot; pré-registro em docs/estimand_table.md')
