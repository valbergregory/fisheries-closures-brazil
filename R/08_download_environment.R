# 08_download_environment.R — esqueleto (primeira entrega 2026-09-03).
# Contrato de interface definido; corpo ativado por fase (ver _targets.R).

download_environment <- function(cfg) stop('Fase 1: NOAA OISST recorte via OPeNDAP/terra')
