# 16_spatial_did.R — esqueleto (primeira entrega 2026-09-03).
# Contrato de interface definido; corpo ativado por fase (ver _targets.R).

run_spatial_did <- function(panel, cfg) stop('Fase 2: DiD com exposição por zona (mona/apa/anéis de distância)')
