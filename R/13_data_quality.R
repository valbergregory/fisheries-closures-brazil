# 13_data_quality.R — esqueleto (primeira entrega 2026-09-03).
# Contrato de interface definido; corpo ativado por fase (ver _targets.R).

data_quality_checks <- function(panel) stop('Fase 1: cobertura AIS, duplicatas, chaves, zeros')
