# 19_spillovers.R — esqueleto (primeira entrega 2026-09-03).
# Contrato de interface definido; corpo ativado por fase (ver _targets.R).

run_spillovers <- function(panel, cfg) stop('Fase 2: anéis de distância + decomposição de leakage (docs, seção 14)')
